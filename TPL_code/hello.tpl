class Person extends Entity {
  int personality;
  
  _construct(int startYear, int endYear, string name, int personality) {
    startYear = startYear;
    endYear = endYear;
    name = name;
    personality_ = personality;
  }
}
